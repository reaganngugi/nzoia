{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open workspace in Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://127.0.0.1:5500",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start workspace server"
    }
  ]
}
